# DOCUMENT 2 - SYSTEM ARCHITECTURE SPECIFICATION

# PoolPredict

## Architecture Principles

### 1. Shared Tournament Data

Tournament data is global and shared across the platform.

Examples:

* FIFA World Cup 2026
* UEFA Euro 2028
* Champions League

Fixtures, scores and results are imported once and reused by all pools.

Benefits:

* Minimal external API usage
* Consistent event data
* Simplified synchronization

### 2. Pool as Tenant Boundary

Pools are the primary tenant unit.

Everything below is pool-specific:

* Members
* Settings
* Markets
* Predictions
* Leaderboards
* AI Reports

### 3. Pool Owners Are Not Bookmakers

Pool owners should not:

* Create fixtures
* Enter scores
* Update results
* Adjust lines manually for every match

Pool creation should be mostly automated.

### 4. Local Database Is Source Of Truth

External providers are data sources only.

Application reads from MariaDB.

Frontend never reads external APIs directly.

---

# High Level Architecture

Frontend (Next.js)
|
v
PoolPredict API (.NET 10)
|
+-- Identity Module
+-- Pool Module
+-- Tournament Module
+-- Prediction Module
+-- Settlement Module
+-- AI Module
+-- Admin Module
|
v
MariaDB 12
|
+-- Redis
|
+-- Hangfire
|
+-- External Providers

---

# Bounded Contexts

## Identity Context

Responsibilities:

* Authentication
* Authorization
* User Profiles

Tables:

* users
* user_external_logins

Roles:

* PlatformAdmin
* PoolOwner
* PoolAdmin
* PoolMember

---

## Pool Context

Responsibilities:

* Pool creation
* Invitations
* Membership
* Settings

Tables:

* pools
* pool_members
* pool_invites
* pool_settings

---

## Tournament Context

Responsibilities:

* Tournament metadata
* Participants
* Events
* Results
* Provider synchronization
* Event management mode

Tables:

* tournaments
* participants
* events
* event_results

Provider-originated tournament records should include provider/source metadata so mock and real provider data can coexist safely.

---

## Prediction Context

Responsibilities:

* Markets
* Point payout configuration
* Predictions
* Points
* Leaderboards
* Point reservation at prediction submit time

Tables:

* markets
* market_options
* predictions
* point_ledger
* payout_configurations
* market_line_overrides

---

## Settlement Context

Responsibilities:

* Market settlement
* Result processing
* Audit trail

Tables:

* settlement_runs
* settlement_logs

---

## AI Context

Responsibilities:

* Weekly recap generation
* Future AI analytics

Tables:

* pool_ai_reports

---

# Database Design

## IDs

Use UUID v7 for primary keys.

Store UUIDs as `binary(16)` where the ORM and database tooling support it.

Otherwise use:

```sql
char(36)
```

Primary keys must be UUID-based.

Avoid auto-increment IDs.

Rationale:

* UUID v7 is time-ordered and works better for indexed inserts than random UUID v4
* UUIDs are safer to expose through APIs and URLs than sequential numeric IDs
* IDs can be generated by background jobs without relying on one database sequence
* A consistent ID strategy keeps cross-module references simpler

High-volume append-only tables, such as `point_ledger`, `settlement_logs` and audit logs, still use UUID v7 primary keys.

For those tables, the primary key is mainly a stable row identity. Most reads should use access-pattern indexes instead of scanning by ID.

Required indexes should be based on query patterns, for example:

* `pool_id`
* `member_id`
* `prediction_id`
* `settlement_run_id`
* `event_id`
* `created_at`
* Composite indexes such as `(member_id, created_at)` and `(pool_id, created_at)`

If profiling later proves UUID primary keys are a real bottleneck for append-heavy tables, an internal `bigint` identity column may be added as a storage optimization while keeping UUIDs as the public and cross-module identifiers.

---

# Market Architecture

Global Event

```text
Argentina vs Brazil
```

Pool A

```text
Over/Under
```

Pool B

```text
Handicap
Correct Score
```

Pool C

```text
Correct Score only
```

Event data is shared.

Markets are pool-specific.

Market line values and payout multipliers come from Point Payout Configuration, not public betting odds.

---

# Market Profiles

## Casual

Auto-generated:

* Correct Score

## Standard

Auto-generated:

Fulltime:

* Handicap
* Over/Under
* Odd/Even
* Correct Score

First Half:

* Handicap
* Over/Under
* Odd/Even
* Correct Score

## Expert

Auto-generated:

* All Standard markets
* Future advanced market types when added
* Advanced configurable profile overrides

Pool owners select a profile. Pool-owner profile overrides are not part of the MVP.

---

# Settlement Engine

Requirements:

* Pure deterministic calculations
* No AI
* No external calls
* Fully unit tested
* Required before MVP release so submitted predictions can resolve into final point balances
* Idempotent settlement so reruns cannot double-credit or double-debit members
* Append-only point ledger entries for payouts, refunds and corrections
* Use prediction snapshots for line values and payout configuration
* Settlement is initiated manually by Platform Admin
* Automatic settlement is not supported for MVP

Supported Results:

* Win
* Lose
* Push
* Half Win
* Half Lose
* Cancelled

Supported Markets:

* Handicap
* Over/Under
* Odd/Even
* Correct Score

Point Rules:

* Prediction stake is deducted immediately when a prediction is submitted
* Negative balances are allowed
* Members with negative balances cannot submit new predictions
* Push and cancelled outcomes refund the submitted stake
* Winning outcomes credit stake plus configured payout
* Losing outcomes keep the submitted stake deducted
* Payout uses the configuration snapshotted when the prediction was submitted

Market Lifecycle:

* Draft
* Open
* Locked
* Settled
* Voided

Event Lifecycle:

* Scheduled
* Live
* Finished
* Postponed
* Cancelled
* Settled

---

# Event Management

Each event has a management mode:

* `provider`
* `manual`

Default mode:

* `provider`

Provider-managed events:

* Updated by provider synchronization when sync is enabled
* Provider sync may update kickoff time, event status, first-half result and full-time result

Manually managed events:

* Controlled by Platform Admin
* Provider synchronization must skip the event completely
* Provider updates are ignored

Platform Admin editable fields for manually managed events:

* Kickoff time
* Event status
* First-half home score
* First-half away score
* Full-time home score
* Full-time away score

Platform Admin can switch an event between provider-managed and manually managed mode at any time.

Manual event management is required for mock testing, manual tournaments, provider outage recovery, result correction and settlement verification.

---

# Point Payout Configuration

PoolPredict does not use public betting odds.

External sports providers provide fixtures, participants, kickoff times, scores and statuses only. They do not provide market payout values for MVP.

Platform Admin manages global default point payout configuration.

Global defaults define:

* Market types enabled by profile
* Default line values
* Supported line sets
* Payout multipliers
* Push, half-win, half-lose and cancellation behavior

When a new match is created, markets are generated from the current global defaults.

Before a market is locked, Platform Admin can change line values for that match's markets.

Pool owners cannot manually manage line values or payout configuration.

Submitted predictions must snapshot:

* Market type
* Selected option
* Line value
* Payout multiplier
* Stake
* Payout configuration version

Settlement must use the snapshotted prediction values instead of the current market configuration. This prevents later admin changes from changing already-submitted predictions.

Future extension:

* Tournament-specific payout configuration can override global defaults
* Pool-specific payout configuration is not part of MVP

Suggested tables:

* payout_configurations
* payout_configuration_market_rules
* market_line_overrides
* prediction_snapshots

---

# Localization

English is the default language and the initial production language.

User-facing labels, market names, system messages, recap labels and admin action names must use translation keys instead of hard-coded display text.

Platform admins can add languages later by entering translations for each English source term.

Missing translations must fall back to English.

Suggested tables:

* languages
* translation_terms
* translation_values

---

# Provider Integration

Interface:

```csharp
public interface IEventProvider
{
    Task SyncTournamentAsync(Guid tournamentId);

    Task<IEnumerable<EventDto>> GetEventsAsync();

    Task<IEnumerable<EventDto>> GetLiveEventsAsync();

    Task<EventDto?> GetEventAsync(string externalId);
}
```

Implementations:

```text
MockProvider
FootballDataProvider
ApiFootballProvider
```

Provider selection should be configurable.

## Parallel Provider Testing Recommendation

Mock and real provider data should be allowed to exist in the same environment for testing, but provider data must be scoped to avoid collisions.

Recommended persistence rules:

* Store provider/source on provider-originated tournaments, participants and events.
* Treat `(provider, external_id)` as the logical external identity for tournaments.
* Treat `(tournament_id, provider, external_id)` as the logical external identity for participants and events.
* Mark mock provider data as test data.
* Ensure sync from one provider never updates rows owned by another provider.
* Expose provider/source in admin tournament and event management screens.
* Provide admin filters for real data, mock/test data and manually managed data.

This allows local full-cycle testing with mock fixtures while preserving real provider data imported from FootballData or future providers.

---

# Background Jobs

## TournamentSyncJob

Responsibilities:

* Import fixtures
* Import schedule changes
* Import participants

## LiveEventSyncJob

Responsibilities:

* Update live scores
* Update status
* Skip manually managed events

## SettlementJob

Responsibilities:

* Not part of MVP
* Future optional automation only after manual settlement workflow is stable

For MVP, settlement is triggered manually by Platform Admin after event results are verified.

## AIRecapJob

Responsibilities:

* Generate weekly pool recap

---

# Polling Strategy

> 24h before kickoff

Every 30 minutes

> 2h before kickoff

Every 5 minutes

> 15m before kickoff

Every 1 minute

Live

Every 30 seconds

Finished

Every 1 minute for 10 minutes

Settled

Stop polling

---

# Security

Authentication:

* ASP.NET Identity
* Google OAuth

Authorization:

Policy-based authorization.

Pool access must always validate membership.

---

# Caching

Redis usage:

* Tournament cache
* Event cache
* Leaderboard cache

Do not cache:

* Settlement execution
* Point ledger writes

---

# Deployment

Services:

* poolpredict-web
* poolpredict-api
* mariadb
* redis
* hangfire

Reverse Proxy:

* Caddy

Deployment:

* Docker Compose

Target:

* Single VPS deployment first
* Scale later if needed
